const db = require("./database"); const sql = "CREATE TABLE IF NOT EXISTS product_categories (id INTEGER PRIMARY KEY AUTOINCREMENT, slug TEXT UNIQUE NOT NULL, name_en TEXT NOT NULL, name_ar TEXT DEFAULT '', name_ms TEXT DEFAULT '', image_url TEXT DEFAULT '', description_en TEXT DEFAULT '', description_ar TEXT DEFAULT '', description_ms TEXT DEFAULT '', sort_order INTEGER DEFAULT 0, active INTEGER DEFAULT 1)"; db.prepare(sql).run(); console.log("PRODUCT CATEGORIES TABLE READY");
